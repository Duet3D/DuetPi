# Settings here override the distribution defaults in
# /usr/share/plymouth/plymouthd.defaults and the default.plymouth alternative,
# so an apt upgrade of rpd-plym-splash cannot revert the Duet boot splash
[Daemon]
Theme=duet3d
