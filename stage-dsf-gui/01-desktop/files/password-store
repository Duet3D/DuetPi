# Use Chromium's built-in password store instead of gnome-keyring. The kiosk
# logs in automatically and never unlocks the login keyring, so any keyring
# access pops a "choose a password for the new keyring" prompt on first boot
export CHROMIUM_FLAGS="$CHROMIUM_FLAGS --password-store=basic"
